# Expo / RN trees often declare peer ranges that lag behind SDK-pinned React (e.g. 19.x).
# npm v9+ treats those as hard errors; this matches what many Expo projects use in practice.
legacy-peer-deps=true
